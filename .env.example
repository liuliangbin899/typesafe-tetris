# TypeSafe Tetris Environment Variables (Optional)
# If not provided, the game automatically uses the local built-in alignment heuristic engine with 100% full features!

# Optional: TypeSafe Official API Key for cloud-based LLM judgment inference
TYPESAFE_API_KEY=your_typesafe_api_key_here

# Optional: TypeSafe Base API URL
# TYPESAFE_API_BASE=https://api.typesafe.ai/v1
